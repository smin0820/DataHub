import styled, { createGlobalStyle } from "styled-components";

export const GlobalStyle = createGlobalStyle`
    /* :root {
        width: 100%;
        max-width: 1280px;
    } */
    body {
        margin: 0;
    }
`;
